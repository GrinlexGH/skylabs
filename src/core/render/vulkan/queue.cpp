#include "queue.hpp"

#include "queue_families.hpp"

namespace Vulkan {
    CQueue::CQueue(const vk::Device& device, uint32_t family_index, vk::QueueFamilyProperties properties, vk::Bool32 can_present, uint32_t index) {
        
    }
}
